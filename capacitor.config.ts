import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01-ioniccomponents',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
